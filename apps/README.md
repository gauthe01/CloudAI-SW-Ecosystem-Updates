# Apps

Fresh application code lives here.

- `web/` will contain the Next.js client.
- `api/` will contain the FastAPI API, worker, agents, database models, and
  migrations.

Nothing in this directory may import from `../Gold` or `../gold`.
